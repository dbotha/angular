'use strict';

describe('myApp.interview module', function() {

  beforeEach(module('myApp.interview'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('ViewController');
      expect(ctrl).toBeDefined();
    }));

  });
});